{
  "name": "hitster-cloud-realtime-mobile",
  "private": true,
  "version": "1.4.2-realtime-v3.1",
  "type": "module",
  "scripts": {
    "check": "node --check index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
